Ruilmijnwoning App v3
Premium / minimal / warm / energetic direction.

Open index.html in a browser.
Keep the ORIGINAL `logo-icon.png` beside index.html. It is referenced directly and is not modified.

Main flow:
Landing -> login/register -> onboarding -> discovery -> detail -> match -> chat -> exchange -> profile.

The discovery card supports mouse/touch dragging:
right = interest/match, left = skip.
